# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Hardhat build output (regenerated by `npx hardhat build`)
artifacts/
cache/

# Ignition deployment records for local/ephemeral networks only.
# Deployments to a real shared network (e.g. sepolia) should be committed
# so the whole team uses the same deployed addresses.
ignition/deployments/chain-1337/
ignition/deployments/chain-31337/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
types/ethers-contracts/
.omm
